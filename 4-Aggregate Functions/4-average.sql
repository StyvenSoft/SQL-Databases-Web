-- SQL usa la AVG() función para calcular rápidamente el valor promedio de una columna en particular.

-- devuelve el número promedio de descargas 

SELECT AVG(downloads)
FROM fake_apps;

-- calcule el precio promedio de todas las aplicaciones en la tabla.

 SELECT AVG(price)
 FROM fake_apps;