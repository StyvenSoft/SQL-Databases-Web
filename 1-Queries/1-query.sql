-- SELECT * indica que queremos seleccionar todas las columnas.
-- FROM table_name indica en qué tabla estamos interesados.

-- Las columnas son id, name, genre, year, y imdb_rating.

SELECT * FROM movies;

